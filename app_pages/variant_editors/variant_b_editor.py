"""Variant B config editor — Hero Card System.

Placeholder until Phase 5 builds the full editor with all editable parameters:
heroes, card pools, skill trees, XP tables, upgrade costs, premium packs, etc.
"""

import streamlit as st


def render_variant_b_editor(config) -> None:
    st.info(
        "Hero Card System editor is under construction. "
        "Switch to **Classic Card System** to use the existing editor."
    )
